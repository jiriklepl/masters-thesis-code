g(bits32 x) -> bits32 {
  bits32 r;

  r = f(x, x);

  return (r);
}

h(bits64 x) -> bits64 {
  bits64 r;

  r = f(x, x);

  return (r);
}
f(auto u, auto w) {
  auto r;

  while:
  if w != u {
    r = u % w;
    u = w;
    w = r;
    goto while;
  }

  return (u);
}
