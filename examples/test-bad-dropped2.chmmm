// tests whether the compiler correctly identifies double dropped statement

drop(auto a) {
    return ();
}

f() {
    stackdata {
        x : y: new bits32;
    }



    dropped x;
    dropped y;

    return ();
}
