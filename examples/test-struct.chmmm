struct F {
  x: bits64;
}

struct G {
  x: bits32;
}

f () {
  stackdata {
    y: F;
  }

  auto a;
  a = [y->x];

  return (a);
}

g () {
  stackdata {
    y: G;
  }

  auto a;
  a = [y->x];

  return (a);
}
