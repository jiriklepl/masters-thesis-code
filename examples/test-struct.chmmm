struct F {
  y: x: bits64;
}

struct G {
  z: x: bits32;
}

f () {
  stackdata {
    y: F;
  }

  auto a;
  a = [y->x];
  a = [y->y];

  return (a);
}

g () {
  stackdata {
    y: G;
  }

  auto a;
  a = [y->x];
  a = [y->z];

  return (a);
}
