{-# LANGUAGE Trustworthy #-}

module CMM.Data.Bimap (module Data.Bimap) where

import Data.Bimap
    ( adjust,
      elems,
      empty,
      fromList,
      insert,
      lookup,
      lookupR,
      toList,
      tryInsert,
      Bimap )
